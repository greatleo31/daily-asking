# Daily Asking V1 实施任务

## 1. 产品基础与 G1

- [x] 1.1 创建 V1 中文 PRD、增量规格、技术设计和 execution 任务映射，明确产品定位、成功标准、非目标与分支边界。
- [ ] 1.2 由项目负责人审阅 proposal、specs 与 design，明确批准 G1 后记录批准信息并合入产品基础分支。

## 2. 视觉基础 `feature/visual-foundation-v1`

- [ ] 2.1 用真实 Flutter 页面构建“今日、证据、工作室、设置”晨昏视觉原型，覆盖加载、空、错、成功和键盘状态，不接入新数据实现。
- [ ] 2.2 在约 360x800 与 412x915 视口捕获亮/暗主题真实截图，核对最长中文文案、触控目标、减弱动态效果和无重叠，并取得项目负责人 G2 批准。
- [ ] 2.3 在 G2 通过后实现颜色、字阶、间距、8px 以内圆角、动效时长和无障碍主题令牌，移除装饰光球与紫色宇宙背景。
- [ ] 2.4 实现四目的地稳定主壳和页面结构，保留草稿、筛选与来源导航上下文。
- [ ] 2.5 运行 `flutter analyze`、现有测试和多视口截图验收，保存视觉分支紧凑验证证据。

## 3. 本地证据存储 `feature/local-evidence-store`

- [ ] 3.1 基于 Flutter 3.44 / Dart 3.12 官方兼容信息固定 Drift、SQLite 与生成工具版本，并创建带版本的记录、标签、问题、答案、产物、来源和迁移元数据表。
- [ ] 3.2 实现仓库接口与 Drift 事务，覆盖记录 CRUD、问题/答案关联、产物/来源关联和来源删除 tombstone。
- [ ] 3.3 实现 `daily_asking.entries.v1` 全量解析、单事务导入、字段/数量核对、完成标记与失败回滚，成功前不覆盖旧载荷。
- [ ] 3.4 将 JournalController、搜索、筛选和 Markdown/JSON 导出切换到新仓库，并保持现有字段和导出兼容。
- [ ] 3.5 新增不超过两组关键测试，证明迁移重复执行不重复、任一步失败会回滚且旧载荷不变。
- [ ] 3.6 运行 OpenSpec 严格校验、`flutter analyze`、完整现有测试和旧数据升级人工验收，保存数据分支证据。

## 4. 每日证据闭环 `feature/daily-evidence-loop`

- [ ] 4.1 实现版本化本地追问规则与问题状态，确保相同输入确定地产生零或一个最高价值问题且不联网。
- [ ] 4.2 实现快速记录应用用例和草稿状态，先提交一句记录，再处理回答、稍后或跳过；保存失败时保留草稿。
- [ ] 4.3 将今日页接入快速记录、单题追问和真实积累反馈，详细字段移入可选编辑路径。
- [ ] 4.4 实现证据图谱投影与证据页交互，展示日期密度、证据完整度、主题、来源回跳和非惩罚空白日。
- [ ] 4.5 实现默认关闭的本地提醒设置，覆盖授权拒绝、时间修改和彻底关闭，不影响核心闭环。
- [ ] 4.6 新增不超过两组关键测试，覆盖追问优先级/跳过语义和保存失败草稿恢复；执行 30 秒固定短句脚本与图谱来源人工验收。
- [ ] 4.7 运行 OpenSpec 严格校验、`flutter analyze`、完整测试和发布 APK 构建，保存每日闭环分支证据。

## 5. AI 证据工作室 `feature/ai-evidence-studio`

- [ ] 5.1 实现 Artifact 仓库和来源关联保存，记录产物类型、提示词版本、模型、调用路径和时间，来源删除只保留无正文 tombstone。
- [ ] 5.2 将四类提示词响应解析为结构化草稿生命周期，区分可用内容、缺口、追问和风险，非法响应不可保存。
- [ ] 5.3 实现工作室的来源选择、出站确认、生成、结构化审阅、编辑、放弃、显式保存、产物列表和来源回跳。
- [ ] 5.4 在设置、披露和错误状态中区分 BYOK 直连与可选网关，并为网络、认证、限流、超时和畸形响应提供可操作恢复。
- [ ] 5.5 新增不超过两组关键测试，证明非法/无依据内容不可保存，以及编辑后的产物保留全部来源关系。
- [ ] 5.6 运行 OpenSpec 严格校验、`flutter analyze`、完整测试和真实/模拟模式人工验收，保存 AI 分支证据。

## 6. 可选 AI 网关 `feature/optional-ai-gateway`

- [ ] 6.1 建立 Worker 的可重复安装、类型检查和测试命令，定义固定上游、允许模型、secret、请求体、超时与 Rate Limiting binding 配置契约。
- [ ] 6.2 实现 `/health` 就绪状态和生成路由的 Bearer 鉴权、JSON/字段/模型/体积校验及稳定前置错误，未通过时不得外呼。
- [ ] 6.3 实现固定上游转发、`AbortController` 超时、无自动重试、限流、成功响应裁剪、稳定错误映射和无正文日志。
- [ ] 6.4 接入 Flutter 网关客户端与设置模式，复用最小来源选择和每次出站披露，保持 BYOK 直连默认。
- [ ] 6.5 新增不超过两组关键测试，证明未授权/非法请求不外呼，以及成功和上游错误按契约映射。
- [ ] 6.6 运行 OpenSpec 严格校验、Worker 类型检查/测试、Flutter 分析/测试和未配置降级验收，保存网关分支证据。

## 7. 开源与发布 `feature/release-v1`

- [ ] 7.1 新增 MIT `LICENSE`、中文 `CONTRIBUTING.md`、`SECURITY.md`、架构/隐私文档和中文发布说明模板。
- [ ] 7.2 使用候选应用真实截图重构中文 README，覆盖定位、核心闭环、架构/数据流、安装、BYOK/网关、隐私、验证、限制和许可证。
- [ ] 7.3 实现仓库外 keystore 的 Android 正式签名配置与受保护发布工作流，生成签名 APK、SHA-256 和 Release 资产且不打印 secret。
- [ ] 7.4 将 GitHub Actions 升级到当前官方支持的 Node 24 版本，并保持同分支去重、分析、测试、APK 构建和上传行为。
- [ ] 7.5 运行 OpenSpec 严格校验、Flutter 分析/完整测试/签名构建、密钥扫描、安装升级和真实截图核对，生成 `v0.2.0-beta` 候选证据。
- [ ] 7.6 由项目负责人执行并批准 G3 后创建 `v0.2.0-beta` 预发布，附 APK、哈希、隐私边界、签名姿态和已知限制。
- [ ] 7.7 完成连续 5 个工作日自用验收，按独立 fix/feature 分支修复阻塞问题并记录真实结果，不编造留存指标。
- [ ] 7.8 使用仓库外正式签名凭据构建并验收 `v1.0.0`，经项目负责人最终确认后创建正式 GitHub Release。

## 8. 规格同步与归档

- [ ] 8.1 核对代码、验收证据和全部增量规格一致，将规格同步到 `openspec/specs/` 并运行 `openspec validate --all --strict`。
- [ ] 8.2 在所有任务、G2/G3 和 V1 发布证据齐全后归档 `product-foundation-v1`，记录遗留风险和下一版本事项。
